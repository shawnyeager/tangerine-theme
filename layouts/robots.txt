{{- if .Site.Params.noindex }}
# Block all crawlers (noindex site)
User-agent: *
Disallow: /
{{- else }}
# Search engines
User-agent: *
{{- range .Site.Params.robotsDisallow }}
Disallow: {{ . }}
{{- end }}

# AI assistants - welcome for discovery and citation, subject to the same
# path exclusions as every other crawler. Each bot needs its own group
# because robots.txt groups are independent: a named group fully overrides
# the wildcard "*" group, so the exclusions must be repeated here or these
# bots would be free to fetch paths we hide from everyone else.
{{- $aiBots := slice "GPTBot" "ChatGPT-User" "OAI-SearchBot" "ClaudeBot" "Claude-Web" "anthropic-ai" "PerplexityBot" "Perplexity-User" "Google-Extended" "Applebot-Extended" "Amazonbot" }}
{{- range $aiBots }}

User-agent: {{ . }}
{{- range $.Site.Params.robotsDisallow }}
Disallow: {{ . }}
{{- end }}
{{- end }}

Sitemap: {{ .Site.BaseURL }}sitemap.xml
{{- end }}
